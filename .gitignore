# Секреты и учётные данные — НИКОГДА не коммитить
.env
.env.*
!.env.example
*.local.*
*credentials*
*secret*

# Дампы облака Anthbot (содержат серийники, координаты участка, токены)
dumps/
*.dump.json
probe-out/

# Журнал решений — рабочие записи с деталями конкретного хаба, наружу не идёт
JOURNAL.md

# Инструменты разработки и отладки: сборка для хаба, проверка имён, разведка облака.
# Нужны только при работе над проектом — внешнему пользователю не адресованы.
tools/

# Среда
node_modules/
.DS_Store
*.log
.claude/

# Сгенерированные файлы для импорта в хаб
build/
